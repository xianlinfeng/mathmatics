package main

import (
	"fmt"
	"testing"

	"github.com/frrad/euler"
)

func Test_stringsSort(t *testing.T) {

	// s := strconv.Itoa(n)
	// sp := strings.Split(s, "")
	// sort.Strings(sp)
	// fmt.Println(sp)
	fmt.Println(euler.SortInt(2134))
}
